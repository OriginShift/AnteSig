# Gate A: Mini-Demo Without Clear402

## Verdict

**PASS**

Behavioral subject SHA: `d8a3dcded5cfc0c0baec2d48726bd492a4dd21a4`

Execution date: 2026-08-08 (Asia/Hong_Kong)

Gate A proves the production Mini-Demo remains a complete, understandable,
fail-closed product loop with `CLEAR402_ENABLED=false`. The Gate change itself
is report-only. Dynamic Bug #87 added the previously missing external-server
smoke entrypoint and was merged before this clean rerun.

This is single-operator QA. No independent-review claim is made.

## Environment

- Node.js: `v22.23.1`
- pnpm: `11.16.0`
- Browser: Playwright Chromium 148
- Clear402: explicitly disabled with `CLEAR402_ENABLED=false`
- Network: not configured
- Subject main quality gate:
  [run 31258670779](https://github.com/OriginShift/AnteSig/actions/runs/31258670779),
  `SUCCESS` on the exact subject SHA under Node.js 22.23.1

## Machine Acceptance

All commands ran from a clean Gate worktree with the required Node.js version.
The production server was intentionally stopped after the health and browser
checks completed.

| Command | Result | Evidence |
| --- | --- | --- |
| `CLEAR402_ENABLED=false pnpm install --frozen-lockfile` | PASS | Lockfile unchanged; all 6 workspace projects installed with pnpm 11.16.0 |
| `CLEAR402_ENABLED=false pnpm build` | PASS | Next.js production build completed; `/`, `/api/health`, and `/api/preflight` emitted |
| `CLEAR402_ENABLED=false pnpm test` | PASS | 42 files, 1,177 tests |
| `CLEAR402_ENABLED=false pnpm test:integration` | PASS | 1 file, 16 tests |
| `CLEAR402_ENABLED=false pnpm test:e2e` | PASS | 6 Playwright tests |
| `CLEAR402_ENABLED=false pnpm start` | PASS | Next.js production server became ready on port 3000 |
| `curl -fsS http://127.0.0.1:3000/api/health \| jq` | PASS | `status=ok`, `clear402.enabled=false`, `network.configured=false` |
| `BASE_URL=http://127.0.0.1:3000 pnpm test:e2e:smoke` | PASS | 7 external-server Playwright tests |

## Browser Acceptance

| Requirement | Result | Evidence |
| --- | --- | --- |
| Desktop/mobile Happy path | PASS | Both viewports show `MANUAL_REVIEW` and the complete three-column evidence story |
| Desktop/mobile amount mismatch | PASS | Both viewports show `STOP`, `DO_NOT_PROCEED_TO_SIGNER`, and `CRITICAL_ALIGNMENT_FAIL` |
| Live failure never becomes Fixture | PASS | Live POST returns HTTP 503 and UI shows `LIVE_UNAVAILABLE`; no Fixture picker or result evidence is substituted |
| Duplicate/stale response protection | PASS | Duplicate submission stays disabled; a cancelled delayed response cannot replace the newer STOP result |
| Raw evidence keyboard access | PASS | Enter opens the dialog, Escape closes it, and focus returns to the trigger |
| Console and layout stability | PASS | No application `console.error`, page error, or horizontal overflow at 1440x900 and 390x844 |
| False mode Credential boundary | PASS | Health reports disabled; DOM and loaded resource URLs expose no Clear402 or Credential controls |

The expected HTTP 503 for `LIVE_UNAVAILABLE` causes one exact Chromium network
diagnostic. The smoke test admits only that browser-generated message and keeps
zero tolerance for every other console error and all page errors.

## Judge Comprehension

Single-operator result: **PASS within the 20-second acceptance window**. No
documentation was opened during the check. The result view answers the required
questions directly:

- `User request` identifies the submitted assets, amount, recipient, and
  slippage.
- `Agent prepared` identifies selection and Capability evidence.
- `Simulation occurred` identifies execution, outcomes, receipts, and warnings.
- The STOP banner and `Why STOP` section identify the exact reason and source
  references.
- The MANUAL_REVIEW banner explicitly says human review remains required and
  that the result is not an approval or authorization.

The external smoke repeats these visible-label assertions so the comprehension
result is backed by executable evidence rather than the report alone.

## Screenshot Evidence

| Artifact | Dimensions | SHA-256 |
| --- | --- | --- |
| `artifacts/screenshots/mini-desktop-happy.png` | 1440x3055 | `82089eab14a33411b5fd84a8cee6b358915c42d4e1d69e51846a41b7dd155ddc` |
| `artifacts/screenshots/mini-desktop-stop.png` | 1440x3353 | `73e64614af8d9fb6403fc1cff835495ed0d133a875e982d7544ae79b7a8b1a50` |
| `artifacts/screenshots/mini-mobile-happy.png` | 390x5761 | `93ff46b12d0ed835ff01ab4dc9ddea939de5cb818e03fdd527cb7cc3a1b1aa57` |
| `artifacts/screenshots/mini-mobile-stop.png` | 390x6282 | `752db5e9072eca2e7ad9b40c14dddef50421765fd13b0fac1d0184a86d166098` |

The screenshots were regenerated by the false-mode E2E run and remained
byte-for-byte identical to the committed acceptance artifacts.

## Scope Audit

- No product, schema, dependency, lockfile, or trust-boundary code changed in
  this Gate.
- Gate A depends on the closed M3 chain: #11 and #36 through #40.
- Blocking Bug #87 is closed, merged, and post-merge CI is successful.
- No unresolved Gate A blocker remains.
